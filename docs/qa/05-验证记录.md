# 审计验证记录

## 1. 验证环境

- 项目：`/Users/joker/Code/RuT0MarkFlow`
- 平台：macOS arm64
- Node（pressure 输出）：v22.17.0
- 当前应用版本：0.1.0
- 审计日期：2026-07-13

## 2. 实际执行结果

### 2.1 前端测试

命令：
```bash
pnpm --dir /Users/joker/Code/RuT0MarkFlow test
```

结果：通过，exit code 0。

摘要：
- Test Files：32 passed / 32
- Tests：245 passed / 245
- Duration：3.50s

说明：测试通过不能覆盖本次静态审计发现的保存竞态、picker 取消清空状态、二次 DOM 注入和并发确认等未测试场景。

### 2.2 前端生产构建

命令：
```bash
pnpm --dir /Users/joker/Code/RuT0MarkFlow build
```

结果：通过，exit code 0。

摘要：
- 1609 modules transformed
- 主 CSS：48.93 kB，gzip 12.36 kB
- 主 JS：553.26 kB，gzip 169.12 kB
- 构建耗时：1.56s

警告：主 chunk 超过 Vite 500 kB 默认阈值；非阻断。

### 2.3 Rust 测试

命令：
```bash
cargo test --manifest-path /Users/joker/Code/RuT0MarkFlow/src-tauri/Cargo.toml
```

结果：通过，exit code 0。

摘要：
- 97 个单元测试通过
- 1 个 pressure integration test 通过
- 0 failed

注意：现有测试仍未覆盖 root 授权伪造、picker token、原子写入、TOCTOU、真实 watcher 生命周期和外部修改冲突。

### 2.4 Rust Clippy

命令：
```bash
cargo clippy --manifest-path /Users/joker/Code/RuT0MarkFlow/src-tauri/Cargo.toml --all-targets -- -D warnings
```

结果：通过，exit code 0；零警告。

### 2.5 版本同步

命令：
```bash
pnpm --dir /Users/joker/Code/RuT0MarkFlow version:check -- --tag v0.1.0
```

结果：通过，exit code 0。

结论：代码和配置内部一致为 0.1.0。

### 2.6 仓库规范检查

命令：
```bash
pnpm --dir /Users/joker/Code/RuT0MarkFlow repo:check
```

结果：通过，exit code 0。

限制：当前脚本没有覆盖跨文档状态一致性、技术设计命令表与注册命令一致性，因此不能把绿色结果视为“所有文档一致”。

### 2.7 压力测试

命令：
```bash
pnpm --dir /Users/joker/Code/RuT0MarkFlow pressure:test
```

结果：通过，exit code 0。

样本与指标：
- Markdown 文件数：1000
- 扫描：4.27 ms
- 索引：1.85 ms
- 总计：6.11 ms
- tree nodes：1014
- 结论：扫描与索引完成，未崩溃
- Clippy：0 warnings

副作用：脚本在项目根目录创建并清理 `.pressure-tmp`，同时写入 `handoff/pressure-test-report.json`。这也是本次 CI 审计记录的问题之一。

## 3. 额外审计命令结果

由 CI/发布审计执行：
- `cargo tree --locked --depth 1`：通过。
- `cargo test --locked`：通过，97 单元 + 1 pressure。
- `cargo clippy --locked --all-targets -- -D warnings`：通过。
- `pnpm exec vitest run --coverage`：失败，缺少 `@vitest/coverage-v8`。
- `pnpm audit --prod`：未完成，当前 npmmirror registry 无 npm audit endpoint。
- `cargo audit`：未运行，本机未安装 cargo-audit。
- Workflow YAML parse：ci.yml、release.yml 均可解析。

## 4. 未执行或无法验证

本轮审计的边界为本地仓库静态审计与现有自动化门禁复核。以下为本地静态审计无法覆盖、需要外部环境进一步验证的域：

- 未启动真实桌面 GUI 做完整人工操作。
- 未实际利用 XSS、asset protocol 或任意路径命令做攻击性验证；结论来自代码路径静态审计。
- 未在 Windows/Linux/macOS x86_64 构建和运行。
- 未推送 tag 或实际创建 GitHub Release。
- 未访问 GitHub Secrets、branch protection、Environment 或 tag protection。
- 未验证 updater 签名公私钥与远端 Release 的实际匹配（本地公钥一致性已复核，见下）。
- 未完成 npm/Rust advisory 数据库扫描。
- 未生成前端覆盖率数据。

## 5. Updater 公钥一致性复核

本次独立复核 updater 公钥一致性：

- `tauri.conf.json` 中 `pubkey` 字段 base64 解码后 SHA-256：`a336f679f6cb4ee7a50a8b6cde5633d2999c01a2eb8377d6b7e7b20be04c6c0c`
- 本地 `.tauri/signing-key.key.pub` base64 解码后 SHA-256：`a336f679f6cb4ee7a50a8b6cde5633d2999c01a2eb8377d6b7e7b20be04c6c0c`
- 结论：两者一致。

仍未验证：GitHub Secret 是否使用该本地私钥；远端 `latest.json` 和 `.sig` 是否由该密钥生成；真实应用内升级验签链路。

## 6. 本地签名私钥权限检查

- 文件：`.tauri/signing-key.key`
- 实际权限：`-rw-r--r--`（0644）
- 风险：同机其他用户可读。
- 建议：设为 `0600`。

## 7. 结果解释

所有核心测试、构建和静态门禁通过，说明当前仓库具备基本可构建、可测试状态；但绿色测试只覆盖现有测试用例，不能反证审计发现不存在。尤其以下问题没有被当前测试捕获：

- 保存/另存为期间继续输入造成数据丢失。
- 取消工作区选择清空现有状态。
- 后端信任调用方 root 和任意 target_path。
- 非原子保存、symlink/TOCTOU。
- CSP 禁用与 HOME asset scope。
- 文档版本状态、命令接口和验收证据漂移。

压力测试脚本输出的"界面在扫描期间保持可操作"属于架构推断（spawn_blocking + loading 态），本测试未运行真实 GUI，未测量 WebView 主线程响应或交互延迟。
