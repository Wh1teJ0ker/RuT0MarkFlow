# v0.1.3 更新日志

## 1. 当前状态
- 状态：`verified_complete`
- 最后更新：2026-07-10
- 最终验收：已通过，23/23 acceptance_criteria ✅，7/7 verification_commands ✅。

## 2. 关键变更

### 后端
- `Cargo.toml` 新增 `tauri-plugin-updater = "2"`，version → 0.1.3
- `lib.rs` 注册 `.plugin(tauri_plugin_updater::Builder::new().build())`
- `commands/updater.rs`（新建）：
  - `check_for_updates`：调用 `app.updater().check()`，返回 `UpdateCheckResult`
  - `install_update`：调用 `update.download_and_install()`，安装后自动重启
  - 5 条命令 shape 测试
- `tauri.conf.json` 新增 `plugins.updater`（已填 signer pubkey、GitHub Releases endpoint、windows installMode passive）
- `capabilities/default.json` 追加 `updater:default` 权限

### 前端服务层
- `types/index.ts` 新增 `UpdateCheckResult` / `UpdateInstallResult` / `UpdateStatus` 类型
- `services/tauri/index.ts` 追加 `check-update` / `install-update` 错误标题和恢复标签
- `services/updater/mod.ts`（新建）：`checkForUpdates()` / `installUpdate()` async 函数

### 前端 UI
- `StatusBar.tsx`：新增「有新版本 vX.Y.Z」可点击指示器（ArrowUpCircle 图标）+「检查更新」按钮（RefreshCw 图标，检查中旋转）
- `App.tsx`：新增 `updateStatus` state、启动期自动检查 effect（mount-once、30s 超时、失败静默）、`handleCheckForUpdates` / `handleInstallUpdate` 回调

### 测试
- `statusbar-update.test.tsx`（6 tests）：更新指示器渲染/隐藏/点击/检查中状态
- `update-check.test.tsx`（4 tests）：启动自动检查、无更新、手动检查、错误处理

### 发布配置
- `.github/workflows/release.yml` 为 updater 签名注入 `TAURI_SIGNING_PRIVATE_KEY` 与 `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`
- GitHub Releases endpoint 修正为 `Wh1teJ0ker/RuT0MarkFlow`

## 3. 验证记录
- pnpm version:check -- --tag v0.1.3: ✅（frontend=0.1.3, backend=0.1.3, workspaceSchema=1.0.0）
- pnpm repo:check: ✅（135 text files scanned, all gates green）
- pnpm test: ✅（178/178 passed, 26 files, +10 new tests vs v0.1.2）
- pnpm build: ✅（tsc + Vite 成功，仅非阻塞 chunk size 警告）
- pnpm pressure:test: ✅（1000 files, excellent quality）
- cargo test: ✅（77 tests: 76 unit + 1 pressure_bench）
- cargo clippy --all-targets -- -D warnings: ✅（0 warnings）
