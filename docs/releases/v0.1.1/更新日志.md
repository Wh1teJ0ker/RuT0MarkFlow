# v0.1.1 更新日志

## 1. 当前状态
- 状态：`complete`
- 最后更新：2026-07-07
- 最终验收：已通过（`status: verified_complete`，`verdict: pass`），验证记录见本文件第 4 节。

## 2. 本轮已完成
- 对外版本号已同步到 `0.1.1`，覆盖 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`version-manifest.json`、`src/version.ts`。
- `scripts/check-version-sync.mjs` 已补强为同时校验版本清单与前端 fallback 常量的一致性。
- `README.md` 已补齐产品简介、技术栈、本地开发、桌面构建、测试命令和 Release 产物说明。
- GitHub Release 工作流已在常规 Tauri bundle 之外新增 Windows 便携版资产，命名模式为 `[name]_[version]_[platform]_[arch]_portable[ext]`。
- 发布前新增 `pnpm repo:check` 仓库规范扫描，覆盖绝对路径、个人工作目录、失效 release tag 链接、图标资源和发布门禁配置。
- 压测入口已固化为 `pnpm pressure:test`，并纳入 GitHub Actions validate 阶段。
- 文档入口已统一到 `v0.1.1`，不再把 `v0.1.2`、`v0.1.3` 作为独立实施轨道。

## 3. 实施记录
- 版本展示相关代码已改为读取 `version-manifest.json` 或共享 fallback 常量，不再保留 `0.1.0` 测试断言。
- `src-tauri/src/commands/health.rs` 的版本断言已改为基于 manifest 取值，避免后续 patch 版本再次漂移。
- 统一异常提示与恢复动作已切到结构化错误映射与统一恢复入口，不再依赖字符串前缀判断。

## 4. 验证记录
- ✅ `pnpm version:check -- --tag v0.1.1`
- ✅ `pnpm repo:check`
- ✅ `pnpm pressure:test`
- ✅ `pnpm test`
- ✅ `cargo test --manifest-path src-tauri/Cargo.toml`
- ✅ `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`
- ✅ `pnpm build`（仅保留 Vite chunk size warning，不阻断构建）
