# 验收记录

> 本文档对照 `docs/01-需求文档.md` 第 11 节"验收标准"逐条标注当前实现状态。
> 状态标记：✅ 已完成 / ⚠️ 部分完成 / ❌ 未完成
>
> 最后更新：2026-07-14

---

## 11.1 主流程验收（7 项）

| # | 验收标准 | 状态 | 证据 | 说明 |
|---|---------|------|------|------|
| 1 | 用户可以选择一个本地文件夹作为工作区 | ✅ | `App.tsx:454` `handleSelectWorkspace` — 调用 Tauri 对话框选择文件夹，通过 `select_workspace` 命令加载 | |
| 2 | 系统以文件夹为驱动加载工作区内容 | ✅ | `App.tsx:207-271` restore 流程 — 从 `load_app_settings` 读取 `recentWorkspacePath` → `load_workspace` 加载工作区 | |
| 3 | 系统可以自动扫描并展示工作区中的 Markdown 索引列表 | ✅ | `scanner.rs:19-238` `scan_dir` 递归扫描；`indexer.rs:14-108` `build_index_tree` 构建树；`workspace.rs:121-173` `select_workspace` 整合 | |
| 4 | 用户可以通过点击 Markdown 索引项直接打开对应文档 | ✅ | `App.tsx:492-530` `handleOpenDocument`；`IndexTree.tsx:63-83` 点击事件绑定与 `activePath` 高亮 | |
| 5 | 用户可以在索引列表中快速打开和切换文档 | ✅ | `document-switch-loading.test.tsx:122-197` same-path fast path + latest-request-wins 测试覆盖快速切换场景 | |
| 6 | 用户可以正常新建、编辑、保存和另存为 Markdown 文档 | ✅ | `App.tsx:331` `handleSaveDocument`；`App.tsx:368` `handleSaveAs`；`App.tsx:418` `handleNewDocument`；`App.tsx:534` `handleContentChange` | |
| 7 | 未保存文档在关闭、切换文档或切换工作区时会提示用户 | ✅ | `App.tsx:70-80` `requestUnsavedConfirm`；`unsaved-confirm.test.tsx` 12 条测试覆盖 save/discard/cancel 三种操作 | |

**主流程小计**：✅ 7 / ⚠️ 0 / ❌ 0

---

## 11.2 模式与渲染验收（7 项）

| # | 验收标准 | 状态 | 证据 | 说明 |
|---|---------|------|------|------|
| 1 | 系统支持无感 Markdown 渲染模式和双栏编辑模式 | ✅ | `App.tsx:100` `viewMode` 状态；`App.tsx:545` `handleViewModeChange` 切换逻辑 | |
| 2 | 两种模式都可以正确显示 Markdown 内容 | ✅ | `ContentArea.tsx:220-241` 无感预览分支；`ContentArea.tsx:244-276` 双栏编辑分支 | |
| 3 | 模式切换后文档内容、保存状态和当前上下文不丢失 | ✅ | `ContentArea.tsx:100-117` `scrollPosRef` 保存/恢复滚动位置；`restore-state.test.tsx:93-121` 恢复后 viewMode 保持 | |
| 4 | 系统可以正确渲染本期要求的常见 Markdown 语法 | ✅ | `renderer.test.ts` 覆盖 13+ 语法类：H1-H6、段落、粗体/斜体/删除线、行内代码、列表、引用块、代码块、表格、链接 | |
| 5 | 系统支持行内公式和块级公式渲染，并能够展示常见 LaTeX 数学公式 | ✅ | `math.test.ts` 行内公式 + 块级公式 + 复杂公式渲染测试；使用 marked + DOMPurify + KaTeX 管线 | |
| 6 | 系统可以在工作区内正确解析相对路径图片和相对链接 | ✅ | `resource.test.ts` 图片资源解析测试；`renderer.test.ts` 链接渲染测试 | |
| 7 | 工作区内 Markdown 链接可以触发应用内打开对应文档 | ✅ | `ContentArea.tsx:69-98` `handlePreviewClick` 拦截 `data-internal-md` 属性链接调用 `onOpenDocument` | |

**模式与渲染小计**：✅ 7 / ⚠️ 0 / ❌ 0

---

## 11.3 索引与状态验收（4 项）

| # | 验收标准 | 状态 | 证据 | 说明 |
|---|---------|------|------|------|
| 1 | 系统可以为工作区建立并使用索引 | ✅ | `scanner.rs:19-238` 扫描 + `indexer.rs:14-108` 建树；`index-tree-expand.test.tsx` 展开状态持久化测试 | |
| 2 | 工作区内文件变化后，文件列表与索引可以更新 | ✅ | `watcher.rs:22-182` start_watcher 使用 notify v6 监听文件变化，300ms 去抖后 emit `workspace://index-changed` 事件；`App.tsx:317` 前端 listen 接收事件触发 `refresh_workspace_index` | |
| 3 | 系统可以显示当前工作区、当前文档、当前模式和当前保存状态 | ✅ | `StatusBar.tsx` 组件渲染状态信息；`statusbar.test.tsx` 验证状态消息展示；`App.tsx:611-619` 传参给 StatusBar | |
| 4 | 系统可以恢复最近一次工作区、最近文档和最近模式，或在恢复失败时安全降级到空工作区状态 | ✅ | `restore-state.test.tsx:34-125` 验证有效工作区+文档恢复、viewMode 恢复、失败降级到空工作区；`App.tsx:207-271` restore 逻辑 | |

**索引与状态小计**：✅ 4 / ⚠️ 0 / ❌ 0

---

## 11.4 性能与稳定性验收（4 项，含子项）

| # | 验收标准 | 状态 | 证据 | 说明 |
|---|---------|------|------|------|
| 1 | 在常规开发机器上，加载中大型工作区时界面保持可操作，不出现持续性假死 | ✅ | `workspace.rs:121-246` `select_workspace`/`load_workspace` 使用 `spawn_blocking` 在线程池扫描；`src-tauri/tests/pressure_bench.rs` 直接调用生产 `scanner::scan_markdown_files` + `indexer::build_index_tree`，最新实测 1000 文件扫描 9.60ms、索引构建 3.17ms（总计 12.77ms）；前端加载态自动化测试覆盖 | PT1 压测：`scripts/pressure-test-workspace.mjs` 生成样本工作区并通过 `PRESSURE_ROOT` 驱动 Rust benchmark；前端测试：`workspace-pressure-loading.test.tsx` 验证 loading→ready 转换 |
| 2 | 以不少于 1000 个 Markdown 文件的工作区作为压力场景时，索引构建过程应可感知且界面可交互 | ✅ | `src-tauri/tests/pressure_bench.rs` 对 1000 个 .md 文件工作区计时：扫描 9.60ms + 索引 3.17ms = 12.77ms 总耗时，质量评级 "excellent"（104,119 文件/秒）；前端 `workspace-pressure-loading.test.tsx` 验证扫描期间显示 loading 态、扫描完成后自动切换到 ready 态、工具条保持可交互 | 架构层面：`spawn_blocking` 确保异步不阻塞 UI；压测由 `scripts/pressure-test-workspace.mjs` 驱动，可通过 `pnpm pressure:test` 复现 |
| 3a | 打开 ≥10000 行文档后应能在可接受时间内显示首屏可视内容 | ✅ | `chunker.ts` 分段渲染 + `content-visibility: auto` + LRU 缓存；`long-document.test.tsx` 4 条测试覆盖首屏立即显示 | T37 已实现：空白行边界分段 + 首批 2 chunk 同步渲染 + 后续 setTimeout 调度 + CSS 虚拟化 |
| 3b | 滚动过程中不应出现持续 1 秒以上的明显卡死 | ✅ | `content-visibility: auto` + `contain-intrinsic-size: auto 500px` 确保滚动不触发全量 layout | T37 实现 |
| 3c | 模式切换过程中不应出现持续 1 秒以上的明显卡死 | ✅ | 模式切换复用已有缓存（`useRender` LRU cache），不触发全量重新渲染 | T37 实现 |
| 3d | 即使存在大量图片、代码块或公式，也应优先保证可视区域可阅读 | ✅ | 分段渲染 + 异步补全 + 图片/公式错误隔离不阻塞整篇渲染 | T37 实现 |
| 4 | 文件读取失败、文件保存失败、索引建立失败、图片加载失败、公式渲染失败时，应用应给出明确反馈且主界面仍可继续使用 | ✅ | `render-errors.test.tsx` imageErrors/mathErrors 隔离；`App.tsx:35-64` saveError/openError 状态管理；`App.tsx:49` workspaceError；`ContentArea.tsx:61` 图片加载失败回调 | 各类错误均有隔离处理，不导致整体崩溃 |

**性能与稳定性小计**：✅ 7 / ⚠️ 0 / ❌ 0

---

## 汇总

| 类别 | 总项 | ✅ 已完成 | ⚠️ 部分完成 | ❌ 未完成 |
|------|------|----------|------------|----------|
| 11.1 主流程 | 7 | 7 | 0 | 0 |
| 11.2 模式与渲染 | 7 | 7 | 0 | 0 |
| 11.3 索引与状态 | 4 | 4 | 0 | 0 |
| 11.4 性能与稳定性 | 7 | 7 | 0 | 0 |
| **合计** | **25** | **25** | **0** | **0** |

**主流程完成率**：25/25 = 100%

**完成判定**：全部 25 项验收标准 ✅ 已完成。11.4.1/11.4.2 已通过 PT1 压力测试补全：1000 文件扫描 9.60ms + 索引 3.17ms = 12.77ms 总耗时，质量评级 excellent；前端 loading 态自动化测试验证界面可交互。

---

## 阻塞任务清单

以下任务已在历轮迭代中实施完成，当前无阻塞任务：

| 任务号 | 名称 | 状态 | 完成轮次 |
|--------|------|------|----------|
| T37 | 超长文档分段渲染/虚拟化 | ✅ 已完成 | 早期轮次 |
| 主题切换 | 深色/浅色主题切换 | ✅ 已完成 | P1 轮次 |
| T39 | 统一异常恢复收尾 | ✅ 已完成 | P1 轮次 |
| 撤销/重做 | Undo/Redo | ✅ 已完成 | P1 轮次 |
| 深色主题 CSS | markdown-dark.css 补全 | ✅ 已完成 | 显示质量轮次 |
| 7 项需求符合度补缺 | 关闭文档 / Cmd+F / 新建确认 / 光标保持 / 恢复重选 / 排序指示 / 扫描进度 | ✅ 已完成 | 最终 HANDOFF 轮次 |
| PT1 | 1000+ 文件工作区压测 + loading 态自动化测试 | ✅ 已完成 | PT1 轮次 |

**所有验收项已全部通过，无剩余 ⚠️ 项。**

---

## 12. v0.1.0 基线结论
- `v0.1.0` 是 RuT0MarkFlow 的**首个对外发布基线版本（融合版）**，已于 2026-07-14 完成全部验收。
- 本版本将 MVP 基线（25/25 项）及全部稳定化、跨平台修复、自动更新、窗口修复、设置界面、圆角图标与 CI 优化的成果融合为单一版本。
- 当前文档中 25/25 项 MVP 验收标准均已有证据。
- 后续版本的迭代方向由产品负责人确定后另行规划，当前不做路线规划。

## 13. 当前版本状态

| 版本 | 版本定位 | 当前状态 | 主要入口文档 |
|------|----------|----------|--------------|
| `v0.1.0` | 融合基线版本（MVP + 稳定化 + 跨平台修复 + 自动更新 + 窗口修复 + 设置界面 + 圆角图标 + CI 优化） | `complete` | `docs/releases/v0.1.0/规划需求.md`、`docs/releases/v0.1.0/更新日志.md` |

说明：
- `v0.1.0` 已完成全部实施、验证与最终验收，验收结论见 `docs/releases/v0.1.0/更新日志.md` 验证结果段。
- 后续版本方向待产品负责人确定后另行规划。

## 14. 稳定化验收快照（v0.1.0 融合版）

| 条目 | 状态 | 证据 | 说明 |
|------|------|------|------|
| 版本同步 | ✅ | `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json`、`version-manifest.json`、`src/version.ts` | 当前对外版本统一为 `0.1.0` |
| README 与发布说明 | ✅ | `README.md` | 已补齐产品简介、技术栈、开发、构建、测试和 Release 产物说明 |
| Release 便携版资产 | ✅ | `.github/workflows/release.yml` | Windows 独立便携版资产，`webviewInstallMode: skip` 真正免安装 |
| 文档口径收口 | ✅ | `docs/README.md`、`docs/releases/README.md`、`docs/releases/v0.1.0/更新日志.md` | 版本状态在 `docs/releases/README.md` 统一索引 |
| 压测与仓库规范门禁 | ✅ | `scripts/pressure-test-workspace.mjs`、`scripts/check-repo-hygiene.mjs`、`.github/workflows/release.yml` | 发布前已加入 `pnpm pressure:test` 与 `pnpm repo:check`，并进入 validate 流程 |
| 跨平台一致性 | ✅ | `src-tauri/capabilities/default.json`、`src-tauri/src/reader.rs`、`src/services/resource.ts`、`src/render/index.ts` | capabilities 配置、BOM/GBK/GB18030 编码兼容、路径分隔符统一、CRLF 规范化 |
| 自动更新 | ✅ | `src-tauri/src/commands/updater.rs`、`src/services/updater/mod.ts`、`src/components/settings/SettingsPage.tsx` | Tauri updater 集成、启动自动检查、设置页手动检查与一键更新 |
| 窗口关闭修复 | ✅ | `src-tauri/src/lib.rs` | 条件 `prevent_close`（仅脏文档拦截关闭） |
| 圆角图标 | ✅ | `src-tauri/icons/app-icon-source.png` 及全部衍生图标 | 四角透明圆角设计 |
| CI 编译优化 | ✅ | `.github/workflows/release.yml`、`.github/workflows/ci.yml`、`src-tauri/Cargo.toml`、`src-tauri/.cargo/config.toml` | rust-cache、LTO thin、并行 lint/rust-check、增量编译 |
| 其余稳定化项 | ✅ | `src/services/tauri/index.ts`、`src/app/App.tsx`、`src/components/content/ContentArea.tsx`、`src/components/sidebar/Sidebar.tsx`、`src/components/statusbar/StatusBar.tsx` | 统一异常提示、恢复入口与全量自动化回归均已通过 |
